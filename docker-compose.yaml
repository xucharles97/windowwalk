services:

  db:
    image: postgres:15.2-alpine
    environment:
      POSTGRES_DB: windowwalk
      POSTGRES_PASSWORD: secret
    volumes:
      - windowwalk-pg-local:/var/lib/postgresql/data
    ports:
      - "5433:5433"

volumes:
  windowwalk-pg-local:
